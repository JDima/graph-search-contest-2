def task6(n: int, edges: list) -> list:
    """
    Parameters:
    :n (int): number of cities
    :edges (list): list of roads where road is pair (a, b). a and b - cities
    :return: probability of losing
    """
    pass
